<div class="container">
  <div class="row">
    <% @posts.each do |post| %>
    <div class="col-4 pt-3">
      <div class="card mb-4 shadow-sm">
        <svg class="review-img card-img-top", style:"height:150px;">
        <%= link_to post_path(post.id) do %>
          <%= image_tag post.images[0].variant(resize: '100x100') %>
        <% end %>
        <div class="card-body" style="border-top:solid">
          <svg class="rounded-circle"><%= image_tag post.user.get_profile_image(50,50) %></svg>
          <h5 class="card-title"><%= post.title %></h5>
          <% if post.is_status == true %>
            <p class="text-success font-width-bold">公開</p>
          <% else %>
            <p class="text-secondary font-width-bold">非公開</p>
          <% end %>
      </div>
    </div>
    </div>
  <% end %>
  </div>
</div>
