<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <table class="table table-hover">
        <thead>
          <tr>
            <th></th>
            <th>グループ名</th>
            <th>内容</th>
            <th>メンバー人数</th>
          </tr>
        </thead>
        <% @groups.each do |group| %>
        <tbody>
          <tr>
            <td><%= image_tag group.get_group_image(100,100), class: "rounded-circle" %></td>
            <td><%= link_to group.name, group_path(group.id) %></td>
            <td><%= group.content %></td>
            <td><%= group.users.count %></td>
          </tr>
        </tbody>
        <% end %>
      </table>
    </div>
  </div>
</div>